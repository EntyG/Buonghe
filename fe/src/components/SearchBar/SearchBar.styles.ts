import styled from "styled-components";
import { Paper } from "@mui/material";

export const BarWrapper = styled(Paper)`
  display: flex;
  align-items: center;
  padding: 2px 8px;
  margin: 0 auto 16px auto;
  max-width: 480px;
  border-radius: 32px;
  box-shadow: 0 2px 8px #0001;
`;
